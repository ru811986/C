#include <stdio.h>

int main() {
    float fahr, celsius;
    int lower, upper, step;
    
    celsius = 0;
    while(celsius <= 100) {
        fahr = (9.0/5.0)*celsius + 32;
        printf("%3.0f\t%6.1f\n", celsius, fahr);
        celsius = celsius + 2;
    }
    
    return 0;
}
